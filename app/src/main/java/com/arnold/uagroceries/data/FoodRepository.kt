package com.arnold.uagroceries.data

import com.arnold.uagroceries.requests.FoodApi
import javax.inject.Inject

class FoodRepository @Inject constructor(foodApi: FoodApi) {

    /* fun searchFoodRecipe(query: String)=
             Pager(

                     config = PagingConfig(pageSize =20 ,maxSize =50 ,enablePlaceholders = false),
                     pagingSourceFactory = TODO("Create Paging Source")

             ).liveData*/
}